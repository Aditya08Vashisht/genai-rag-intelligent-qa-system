# ===========================================
# GenAI RAG Intelligent Q&A System
# Environment Configuration
# ===========================================

# LLM Provider: "ollama" (local, FREE) or "gemini" (requires API key)
LLM_PROVIDER=ollama

# Ollama Settings (FREE - no API key needed!)
# Download from: https://ollama.ai/download
# Then run: ollama pull llama2
OLLAMA_MODEL=llama2
OLLAMA_URL=http://localhost:11434

# Google Gemini (Optional - if you have an API key)
# Get your free API key at: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=

# Embedding Model (FREE - runs locally via HuggingFace)
# No API key needed - uses sentence-transformers locally
EMBEDDING_MODEL=all-MiniLM-L6-v2

# Vector Database (FREE - ChromaDB local)
CHROMA_PERSIST_DIR=./data/chroma_db

# Application Settings
APP_NAME=GenAI RAG Q&A System
DEBUG=true
LOG_LEVEL=INFO

# API Settings
API_HOST=0.0.0.0
API_PORT=8000

# Data Storage
UPLOAD_DIR=./data/uploads
MAX_FILE_SIZE_MB=10

# Data Storage
UPLOAD_DIR=./data/uploads
MAX_FILE_SIZE_MB=10
